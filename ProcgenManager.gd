class_name ProcgenManager extends Spatial


# Declare member variables here. Examples:
# var a = 2
# var b = "text"



func player_sees(portal: Spatial):
	if uncollapsed_waveforms.has(portal):
		print("yep!")

# Called when the node enters the scene tree for the first time.
func _ready():
	for portal
	uncollapsed_waveforms.append()
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
